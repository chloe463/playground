overwrite: true
schema: "./schema.json"
documents: "src/**/!(*.d).{ts,tsx}"
generates:
  src/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
hooks:
  afterAllFileWrite:
    - prettier --write
