import React from "react";
import styled from "styled-components";

export const NewQuestionnaireForm: React.VFC = () => {
  return (
    <Base>
      NewQuestionnaireForm works!
    </Base>
  );
};

const Base = styled.div`
  padding: 0 24px;
`;
